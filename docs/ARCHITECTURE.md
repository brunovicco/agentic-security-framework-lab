# Architecture

This library exposes `agentic_lab` from `src/agentic_lab`. Document its public API,
compatibility policy, internal modules, and external dependency rationale here. Package-specific
default-deny boundaries can be declared under `[tool.engineering-harness.architecture]` in
`pyproject.toml` without imposing Clean Architecture on every library.
